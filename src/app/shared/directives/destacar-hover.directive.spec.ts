import { DestacarHoverDirective } from './destacar-hover.directive';

describe('DestacarHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DestacarHoverDirective();
    expect(directive).toBeTruthy();
  });
});
